const app = new Vue(                    // una nuoova instanza di un oggetto di classe vue con varie caratteristiche inserite al suo interno
    {
        el: "#app",                         // l'id di partenza di vue e selezionato nel dom html

        data:{
            message: "" 
        }

    }
)